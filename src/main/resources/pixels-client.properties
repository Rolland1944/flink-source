# Pixels Server Configuration
pixels.server.host=172.31.20.9
pixels.server.port=9091
schema.name=pixels_bench_sf10x
table.name=customer

# Schema Definition (format: col1:TYPE,col2:TYPE)
# Supported Types: INT, BIGINT, STRING, FLOAT, DOUBLE, BOOLEAN
source.schema=custid:INT,companyid:INT,gender:STRING,name:STRING,age:INT,phone:STRING,province:STRING,city:STRING,loan_balance:FLOAT,saving_credit:INT,checking_credit:INT,loan_credit:INT,Isblocked:INT,created_date:DATE,last_update_timestamp:BIGINT

# Sink Type (paimon or iceberg)
sink.type=iceberg

# ==========================================
# Paimon Configuration
# ==========================================
paimon.catalog.type=paimon
# Warehouse Path for S3
paimon.catalog.warehouse=s3://home-dongyang/paimon/
paimon.database.name=public
paimon.table.name=paimon_orders

# ==========================================
# Iceberg Configuration
# ==========================================
# Type: glue (AWS Glue), hadoop, or rest
# Using AWS Glue as catalog for cloud-native setup
iceberg.catalog.type=glue
# Warehouse Path for S3
iceberg.catalog.warehouse=s3://home-dongyang/iceberg/
iceberg.database.name=public
iceberg.table.name=iceberg_orders

# ==========================================
# Flink Job Configuration
# ==========================================
checkpoint.interval.ms=10000
